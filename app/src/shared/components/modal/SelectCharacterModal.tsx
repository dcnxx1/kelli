import { View, StyleSheet, Modal } from "react-native";

export default function SelectCharacterModal() {

  return <View style={ModalStyle.Modal}></View>;
}

const ModalStyle = StyleSheet.create({
  Modal: {},
});
